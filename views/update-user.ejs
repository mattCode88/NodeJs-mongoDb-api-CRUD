<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update User</title>
</head>
<body>

    <h1>Pagina di Update User</h1>
    <a href="/users/show-users">Torna a tutti gli utenti</a>
    <div>
        <form action="/api/users/<%= user._id %>?_method=PUT" method="POST">
        
            <div class="form-group">
                <label for="name">Name</label>
                <input type="hidden" name="id" value="<%= user._id %>">
                <input type="text" name="name" value="<%= user.name %>" placeholder="Matteo Meago">
            </div>
        
            <div class="form-group">
                <label for="email">Email</label>
                <input type="text" name="email" value="<%= user.email %>" placeholder="example@email.com">
            </div>
        
            <div class="form-group">
                <label for="passwd" class="text-light">Password</label>
                <input type="text" name="passwd" value="<%= user.passwd %>" placeholder="miaPassword">
            </div>
        
            <div class="form-group">
                <button type="submit">Save</button>
            </div>
        
        </form>
    </div>
    
</body>
</html>